# H172 context pack — full-cofactor grid resonance

**Canonical status:** `OPEN`.  The exact claim is in
[`../CLAIMS_LEDGER.md`](../CLAIMS_LEDGER.md). Phase 31 v2 proves the all-radius
short-leaf repair, Phase 32 adds triple-hit capacity and the full noncoprime
cofactor, and Phase 36 adds root/event structure and excludes all critical
areas through 229. Its current boundary is in the
[`Phase 36 audit`](../../research/audits/root-event-polynomial/REPORT.md).

## Target

Convert P194's sharp-frontier `o(L)`-defect singleton grid into a strict
arithmetic contradiction.  The theorem must retain full-`D` noncoprime
arithmetic and transport between the least-value and discrepancy-minimum
rotations.  H133 separately needs a reason arbitrary hypothetical cycle
families approach this sharp frontier, or a theorem covering profiles strictly
above it.

## Required inputs

- P133/P134 for the positive-cycle valley and critical/noncritical split.
- P156/P157 for the all-gcd reduced profile and state separation.
- P162--P165 for the earlier area/support dispersion baseline.
- P166/P167 for exact zero-token transport and level inventory.
- P168/P169/P170 for the sharp constant and equality scales.
- P171 for the corrected multilevel polynomial decomposition.
- P173 for all-cut coprime nonvanishing and exact 2-adic valuation.
- P174/P175 for resonance and fixed-area eventual exclusion.
- P176 for the all-gcd maximum-state bound.
- P179/P180 for the direct factor-set and positive-cycle separation bounds.
- P181/P182 for the improved area constants.
- P183/P184 for the repaired equality scales and singleton normal form.
- P185/P186 for static disjoint singleton extraction and double-hit factor
  counting.
- P187/P188 for the current area constants.
- P189 for the local exact-two structure relative to the residual set.
- P190 for the exact grid recurrence and conditional low-denominator form.
- P191/P192 for all-radius short-leaf pruning and finite double-hit counting.
- P193 for `xy>=2ell`, the repaired area proof, and vanishing normalized slack
  at equality.
- P194 for the global `o(L)` bad-window conclusion at the sharp frontier.
- P195/P196 for the last leading factor-cardinality layer and its optimized
  area necessity.
- P197/P198 for full-`M_d` block decomposition and primitive block
  oscillation.
- P199 for the cancellation-free positive support arc and critical area-six
  reduction `d<=s<=6`.
- P200--P214 and E46--E52 for the critical `A>=230` bootstrap, refined scalar
  filters, root-capacity elimination, and closed H200 target.
- P211/P212 for root localization, factor/gap control, and bounded-root
  one-hit capacity.
- P215--P217 for the positive Mersenne recurrence and coprime event-polynomial
  support, norm, modular equivalence, and arc inequalities.
- EXT17 only for the critical multiplier-gap specialization.
- P137/P140/P147/P154 for existing resultant and all-gcd divisor structures.

## Fastest falsifiers

- NG37: finite equality between the descent-density and triangular bounds.
- NG38: endpoint correction is mandatory in every weighted `l1` argument.
- NG34: a sparse three-root pattern can have direct modular gcd one.
- NG35: scalar coefficient comparison reverses at critical area six.
- NG36: least-value and discrepancy-minimum rotations need not agree for a
  positive rational shadow.
- Exact tall, plateau, isolated, near-extremal, and seven-grid Phase 28
  profiles; both negative cycles; the full q<=8 all-gcd corpus.
- E41's 93,629 arc cuts, including tied largest gaps and the NG38 endpoint.
- NG39: the exact span term cannot be dropped.
- E42's full-width direct-factor and secondary-peak corpus.
- NG40: one normalized Phase 31 inequality does not imply a global grid.  The
  new proof must visibly use every fixed `R` before `R->infinity`.
- E43's width-two, multipeak, near-grid, and residual-heavy profiles.
- E44's 10,485 rotations and 522,870 radius/width cases, including the
  generator/verifier pruning-order difference.
- E45's 522,870 triple-hit capacities and 2,936 positive full-cofactor support
  arcs; its primitive noncoprime positive-integral premise count is zero and
  must not be used as existence or exclusion evidence.
- E46--E50's exact finite-height, decoder, and scalar frontiers; do not infer a uniform
  all-area theorem from repeated finite tiers.
- NG41: the simultaneous direct-factor/residual triple-hit sieve admits the
  exact area-229 scalar tuple; another finite tier is not a growing-area proof.
- P213 eliminates NG41's exact tuple, but the area-230 neighbor survives;
  another scalar tier is still not a growing-area proof.
- NG42: cycle and P206 decoder roots have opposite orientation. It is an H89
  warning and must not be erased by a common profile notation.

## First useful experiment

Start from P194's approximate period `n+1` beyond P214's finite area floor and
P211's level-one root intervals. Prove a uniform dichotomy: sparse/short roots
must violate P212, while the complementary dense branch must make P215/P216's
positive event divisor incompatible with P197's full cofactor. Encode paired
singleton endpoints in P171 and seek a nonzero full-`D` resultant whose error
is controlled by the `o(L)` bad starts. The first falsifier must allow sparse
but adversarially placed bad starts rather than assuming interval-uniform
errors. Reject candidates on NG34--NG42 and the Phase 28--36 corpora before
scaling. Separately test whether the cycle inequalities force a least-area
hypothetical sequence to the P193 frontier.

## Acceptance boundary

A useful theorem must turn the P194 `o(L)`-defect grid into a strict
subleading margin, use P197's
full-`M_d` noncoprime arithmetic, and transport
explicitly between the two rotations.  Another nonvanishing statement,
stronger area constant, another finite scalar floor, or approximate-period
identity is not sufficient.

## What this result would not separately establish

Closing H172 would advance only the positive-cycle branch.  H54, H70, H72,
H89, H104, H105, H112, and the full Collatz conjecture remain separate.
`proves_collatz=false`.
