# H112 context pack — nonzero lifts on infinite safe geodesics

> 2026-09-10 [EXT08 scope audit](../../EXT08_SCOPE_AUDIT_RESULTS.md): EXT08 is now `OPEN`,
> not an accepted external premise and not a refuted theorem statement.
> P119/P121/P123/P124/P128 retain their conditional historical routes;
> P127, internal permanent-safe reduction and Phases 42--44 remain intact.
> Older artifact status snapshots are historical, not current claim authority.


Canonical status: `OPEN`. The exact statement is the H112 row of
[`../CLAIMS_LEDGER.md`](../CLAIMS_LEDGER.md). This is a scoped retrieval pack;
the proofs are in Phase 19 and the
[`Phase 41 audit`](../../research/audits/shifted-decoder-all-q/REPORT.md),
which preserves and simplifies the Phase 40 reduction. The latest
[`Phase 42 audit`](../../research/audits/mechanical-capacity/REPORT.md)
adds a specific ordinary-source exclusion without resolving H112.

## Safe-root-frontier boundary

Read the [safe-root audit](../../research/audits/safe-root-frontier/REPORT.md).
P281 retains a Q cap when replacing an unsafe improvement by a safe suffix,
but changes its source. P282 proves sharp correction extrema and O(q)-width
windows; fixed-Q no-hit is not all-Q. P283's all-length LEX certificates
require every short prefix (NG51, S=14), and allow normalization equality
only for z>=S. The root-count bound controls no subsequent trace time.
P284 preserves every prefix of a hypothetical global (height,source) minimum
and permits a finite nonempty minimum-height unresolved set. No effective
last cut or successful ancestor follows. Next force a source/carry/lift-linked
LEX improvement or exclude compatible certificates at one fixed source.
B1 is now audited; B2--B6 and the dyadic exploration remain unaccepted.

## Synchronized-shadow boundary

The [shadow audit](../../research/audits/synchronized-shadow-closure/REPORT.md)
adds P274--P280/E64/NG50. A strictly smaller normalized-height source may have
a different future, not coalesce with the original left path. P276's
two/three-peer classification requires identical per-block L and q; P279
only blocks one fixed affine conjugacy. Fast falsifiers: the even S+3 peer,
real conjugacies with no integer pair, and P274's valuation budget.
Next require source-linked, state/time/Q-aware coverage and closure. Do not
combine opposite height-class tie-breakers or promote B2--B6 backlog.

## Phase 44 zero-edit variation input

The [critical-safe-mass supplement](../../research/audits/critical-safe-mass/REPORT.md)
adds P271--P273: a uniform critical-mass ceiling for distinct permanent-safe
ordinary sources and a renewal-index log-log defect bound. It does not count
P80 addresses or force an improving ancestor. The needed new input is a
literal source/lift/ancestry supply contradicting that ceiling; NG49 shows
mass alone allows infinite sets. P270 gives an explicit finite normalization
option but no effective last join. Do not infer permanent membership from
the infinite finite-prefix filter C_b.

The [transient supplement](../../research/audits/transient-sparsity/REPORT.md)
adds P269/P270: distinct finite input paths have Y<256S without a future
hypothesis; non-dropping paths with S>=2^19 have Y<13S. Therefore all strict
competitor sources lie below those cutoffs and explicit K padding is valid.
Repeated targets only have a cycle-deletion witness; NG48 forbids a universal
source-pool extension to them. A new theorem must still control join times,
carry or cross-source ancestry. NG46 defeats monotone-defect shortcuts.

Read [`P262--P267`](../../research/audits/edit-capacity/REPORT.md).
With distinct positive full input states and exact height n=H+K, every
integer r>=0 gives EDIT-CAP. Finite safety is not required. On a positive
permanent-safe orbit, fixed k>=2 and H=o(q^(1/k)) imply liminf HD/q>=k alpha/2;
D=o(q) forces H/log_2(q)->infinity. These are lower costs, not contradictions.
A next proof must use one fixed ordinary source's carry/lifts/ancestry to
give an incompatible upper cost or force an actual dominating ancestor.

P265's oscillating u has H=2log_2(q)+O(1), HD/q->alpha/2, and old pure CAP
zero at every depth. Its prefixed w has no higher-weight same-vertex tail.
P266 nevertheless excludes their positive ordinary sources by EDIT-CAP,
without P221 or monotone P254. The CAP origin u and w's shifted vertex are
different; neither has established all-same-Q/all-Q geodesicity. E60 checks
nonvacuous low-edit windows and eight finite repeat-and-split certificates;
occurrence counts are not distinct-word counts. P267 is a separate finite
pre-repeat sparsity extension, requiring tail truncation. H112 remains OPEN.

## Phase 43 certificate boundary

Read [`P257--P261`](../../research/audits/normalization-barrier/REPORT.md).
For a literal target S->y with coefficient c, all strict improvements start
at z<y/c. The new mode constructs a finite rational potential, extended by
y/x outside its stored set, to certify no gain for **all** positive sources,
lengths and Q. This includes even, unsafe, empty and cyclic paths. A valid
final-endpoint certificate covers every earlier prefix. Candidate and work
limits return UNKNOWN; Phase 41's bounded no-hit result is still not a certificate.

P259 supplies termination only for each fixed prefix of a hypothetical
global minimum over all positive nonperiodic sources, not arbitrary targets
or merely one future class. P260 uses inherited E46/E54 to prove Ycal(S)<13S;
it bounds sources, not join times. Thus a hypothetical minimum retains a
finite certificate at every prefix. A useful next theorem must contradict
this compatible family using one fixed ordinary source, ancestry/carry, or
a P253 variation bound; finite certificate production itself is not exclusion.

Fastest new falsifier: NG46, `7 -> 11 -> 17 -> 26 -> 13`, has every prefix
all-Q coefficient-maximal but a_2=1>a_3=0. The longer (703,80) certificate
retains a_43=6>a_44=2 and all 81 prefix comparisons. This refutes finite
geodesic monotonicity, not an infinite-minimizer-only claim. Preserve both.

## Target and implication

Prove that every infinite coefficient-safe all-prefix same-Q-geodesic branch
has infinitely many nonzero canonical accelerated source lifts. P115 says
that eventual zero lifts are equivalent to one fixed positive ordinary source.
Finite zero runs and a coherent 2-adic source are insufficient.

P242/P243 give an internal equivalence with exclusion of the entire
nonperiodic branch. P221 gives finite normalized height Ycal(x)>x for every
positive nonperiodic source, even or odd. If any exist, finite sublevels give
a global minimizer. The cocycle directly makes every prefix safe and
coefficient-maximal against all positive literal competitors, including
unsafe and even ones of any Q/length. The canonical-positive bridge gives
all-prefix same-Q geodesicity, and P115 gives eventual zero lifts. Conversely,
failure of H112 gives a positive ordinary permanent-safe source by P115;
it cannot become periodic because a positive cycle's repeated coefficient
contracts. Neither side of this equivalence is established.

This implication uses neither X02 nor a finite source cutoff. P240 remains
the historical `CONDITIONAL` X02-based route. H112 stays `OPEN`; positive
cycles remain a separate obligation under H133.

## Exact state and useful next experiment

Combine P115's source residue/lift digit with P235's current-state carry,
ordinary source order, and P238's shifted-correction DAG. The latter starts
with `w=1^R0v`, tail length `ell=|0v|`, and odd `J=D(w)/2^R`.
P248 completely decodes (L,q,D), but does not force any successful ancestor.
At fixed (ell,J), P249 permits gain k>0 and 0<=h<=min(R,k), with
`z=(S+1)/(2^h3^(k-h))-1>0` integral and alternate run R-h. Keep even h=R
competitors. A least ordinary counterexample needs no competitor safety;
other selected-representative arguments must retain their safety or valley
hypotheses. P250 caps possible weights exactly; NG44 forbids filling holes.

P251/NG45 give a permanent-safe critical formal word with no higher-weight
tail at its shifted vertex at any depth. P255 now excludes its ordinary
positive source, while its formal/2-adic no-gain property remains valid;
full geodesicity is not established. Therefore a forced-ancestor theorem needs
ordinary-source arithmetic or broader comparisons, not the analytic conditions
alone. The finite oracle uses D'=2^L'(y+1)-3^q'(z+1), all q'<=L', and the
exact bound z<y/c_target. Its L' cutoff is declared, not inferred from the
target length. Preserve 27 --110-->31 and the other cross-Q controls.

NG43 now refutes omission of that safety test. Complete enumeration of tails
starting with zero through length 25 finds no failure through 24 and four
ordered failing pairs at 25. The lexicographically first pair has
`J=173991363` and tails `0110110110110010110101101` /
`0011111111111011100001100`; both require initial run four, so reducing the
run for the higher-weight tail breaks safety. The supplied `J=166692291`
witness is retained. Strict-valley rescue remains valid.

P247 proves eventual maximum-ratio stabilization even over all Q and lengths,
using finitely many possible positive sources. No effective last join follows.
P244 bounds the nondecreasing integer redundancy
`r_q=E_q-ell_q(x_q)<log_2 mathcal Y(S)`. Its limiting value exists, but no
effective last-jump index follows. Replacement decreases normalized height
by at least a factor two; safe redundancy-one replacement gives equality.
Ordinary source order need not decrease. P245's cloud is collision-free;
P246's weighted moment follows directly from P221 on the endpoint orbit and
is not a new independent obstruction to source stabilization.

P237 requires `k>=Q-O(1)` for nonzero dominating lifts with local odd counts
`exp(o(Q))`. Seek a growing family of collisions satisfying the prefix carry,
or a recurrence forcing nonzero source lifts. An explicit uniform rule is
required before extending the finite depth merely to find more collisions.
The nonnegative k,s theorem must not silently exclude higher-Q ancestors.

## Fastest falsifiers

- Identity `a=d=1` lifts at every depth; P236 excludes identical rules.
- `a=1,d=101,S=1` has zero carry and equal endpoint 2 on the trivial cycle.
- NG24: prefix `110` separates `11101` and `111100` endpoint residues.
- NG22: formal companions and coherent 2-adic sources survive analytic tests.
- Source 167: a long finite terminal zero-lift run precedes coefficient failure.
- NG43: greater tail weight at the same DAG vertex need not preserve safety.
- NG44: ell=14,J=24573 has weights {1,2,5,6}; no gain interpolation.
- NG45: w=110111111u has J/3^t<3 at every tail prefix, but P255 excludes
  its positive ordinary source; it is not an H112 refutation.
- P254 applies only to nondecreasing defect. P253 permits downward variation;
  no geodesic monotonicity or contradicting variation budget is known.
- P256's q=22 negative-carry witness must survive in addition to old NG28.
- Source7 has no mixed same-vertex rewrite at any time, but converges.
- 27 --110-->31 dominates the empty target with greater Q and length.
- A finite Bellman plateau does not identify the last redundancy increase.
- All six mandatory families, including A=11101, B=1100 and `A^rB^s`.

## Acceptance boundary

Phase 42 proves joint mechanical capacity only for positive literal distinct
states with strict height control. Its 2,048-source ordinary sample has zero
counted factors, so its CAP check is vacuous for packing. Eight direct finite
repeat-and-split certificates independently exclude the specified u prefixes
through S<=2^1024; the all-source exclusion is P254/P255's written proof.
Next connect lifts, source order or ancestry to an upper H/D budget that
contradicts P253/P254. Excluding this one model is not enough; faster monotone
and oscillating defects remain. Historical Phase 41 artifacts are unchanged.

Phase 41 adds an independently reconstructed L<=14 decoder/fiber audit,
4,192 mixed rewrites, 512 formal odd steps and 718 ancestor hits at L'<=10.
These are not all-Q no-hit or ordinary-positive-source certificates. The
formal all-depth result comes from the written square-shell proof, not samples.

Phase 40 independently reconstructs all 33,554,431 tails through length 25,
1,024 Bellman rows, and 12,954 cloud rows. These declared finite ranges do
not prove all-depth geodesic extinction or recognize a stable zero-lift source.
P236 excludes only fixed direct lifts, not compositions or other proof uses
of finite dictionaries. A full H112 result needs an all-depth argument with
ordinary positivity and canonical lifts. A bounded finite graph must prove
that it represents the complete language. `proves_collatz=false`.
