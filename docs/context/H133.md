# H133 context pack — positive-cycle resultant/energy barrier

**Canonical status:** `OPEN`. The exact statement is the H133 row in
[`../CLAIMS_LEDGER.md`](../CLAIMS_LEDGER.md). The detailed derivation is in the
[`Phase 22 audit`](../../research/audits/cycle-resultant/REPORT.md), with the
current double-hit area and grid/residual frontier in the
[`Phase 31 audit`](../../research/audits/double-hit-transport/REPORT.md).

## Target and boundary

Exclude every primitive positive nontrivial odd cycle. This closes only the
cycle branch of P69; H70 and H72/H112 remain separate nonperiodic obligations.

## Required inputs

- P133: at the cycle minimum, every proper coefficient prefix exceeds the
  final multiplier and `m(1-lambda)<q/3`.
- P134: cycle G170/H170 split; the G170 branch has
  `L=ceil(q log_2 3)`.
- P135: coprime residue-indexed slope profile and modular slope root.
- P136: area zero gives only the trivial cycle.
- P137: full-`D` resultant divisibility and radial-energy upper bound.
- P138: every positive coprime area-one profile is excluded; EXT05 is used
  only for critical `q>=13`.
- P139/EXT15: conditional Christoffel `C_min` gap.
- P140: weaker grouped modulus/resultant for noncoprime slopes.
- E28: every positive start below 300000 reaches one.
- E34: finite falsification corpus, not an asymptotic input.
- P144/P145: coprime cycle edit area, triangular height, cyclic factor
  complexity, and primitive-positive state separation.
- E35/NG32: bounded Phase 23 regression and the terminal critical-word
  boundary warning; neither supplies an all-area source inequality.
- P147/P148: exact sparse circular-arc divisor and complete area-two/profile
  recurrence classification.
- P149: every hypothetical positive nontrivial coprime cycle now has defect
  area at least three; the critical large-q part uses EXT05.
- P150: fixed-area noncritical profiles are eventually excluded when their
  sparse lift is nonzero.
- E36/NG33/H147: complete area-two remainder, finite area-three paired-support
  diagnostics, and the warning that generic seven-point cardinality is too
  weak.
- P151: cycle support controls literal Hamming distance and cyclic factor
  complexity; at area three the three shapes have exact distances 2, 4, 6.
- P154/P155: exact coprime resonant-grid resultant theorem and complete
  exclusion of the critical seven-grid roots `Q,2Q,3Q`.
- E37/NG34: independent finite resonance data and the exact `q=63322`
  counterexample to every-profile paired q/L arc threshold.
- P156/P157: reduced-slope edit area and ordinary-state factor separation for
  every gcd class.
- P158/H147: every critical positive cycle has `A_*>=6`; the former coprime
  area-three obligation is closed.
- P159: every noncritical positive cycle has `A_*>100000` internally.
- P160: conditional X02 amplification to `A_*>5*10^15`.
- P161/E38/NG35: exact slope/area phase diagram, all-gcd bounded regression,
  and the coefficient reversal preventing the scalar proof from excluding
  critical area six.
- P162/P163: a polynomial multiplier gap forces `A_*=Omega(q^(2/3))`, and
  the noncritical gap is internal.
- EXT17/P164: Matveev's external two-logarithm theorem supplies the critical
  polynomial gap, so the area dispersion holds globally along unbounded
  hypothetical positive-cycle sequences.
- P165: support satisfies Hamming distance at most `2s_*`,
  `p_cyc(n)<=(2s_*+1)n+1`, `h_*<=s_*`, and
  `s_*=Omega(sqrt(q))` across every gcd class.
- E39/NG36: bounded exact regression plus the positive rational
  `e=(1,3)` obstruction to identifying least-value and discrepancy-minimum
  rotations.
- P166/P167: exact balanced zero-token transport and level-component descent
  replace support-only editing by the sharper variables `J,h,A`.
- P168--P170: the sharp slope-dependent area constants and unique
  near-extremal transport/height scales.
- P171/E40: corrected multilevel endpoint polynomial and independent
  transport-corpus/synthetic regression.
- NG37/NG38/H172: finite strictness and endpoint-free estimates are refuted;
  the near-extremal resonance/resultant bridge remains open.
- P173--P175: every coprime P147 arc is nonzero and every fixed defect area is
  eventually excluded, but growing area remains compatible with the exact
  resonance inequalities.
- P176/P177: every gcd class has a reduced-slope maximum-state bound, giving
  the internal reduced-period floor `q0>=971` for positive integer cycles.
- P178/E41: X02 conditionally raises that floor to `72057431991`; E41 is a
  bounded independent arc/state regression, not asymptotic evidence.
- P179--P182: exact direct token transport improves the global area constant
  by `2^(2/3)`; only the critical specialization retains EXT17.
- P183/P184/E42/NG39: repaired equality rigidity, near-uniform singleton
  transports, independent finite regression, and the mandatory span term.
- P185--P190/E43/NG40: static singleton extraction, double-hit factor
  counting, the current area constant, exact grid identities, independent
  finite regression, and the obstruction to global grid promotion.

## Known falsifiers and traps

- `D|B` forces literal finite parity legality but not primitive period. The
  bounded integral rows include powers of the trivial cycle.
- The cycles `(-5,-7)` and
  `(-17,-25,-37,-55,-41,-61,-91)` must remain valid algebraic/resultant
  regressions; a positive-cycle theorem must state where positivity enters.
- A rational affine fixed point is not an integral cycle.
- Profile coordinates are indexed by residues `H_j mod q`, not time `j`.
- The coprime minimum rotation is unique; this fails in general when
  `gcd(L,q)>1`.
- `D_0|D` gives only a weaker noncoprime condition. Do not replace `D` by
  `D_0` inside a claimed full exclusion without proving the loss harmless.
- P158 supersedes the positive-cycle small-area conclusion across all gcd
  classes, but says nothing contradictory about critical area six or large
  noncritical area.
- Defect area may concentrate in a few deep coordinates; P144's triangular
  height and factor bounds do not by themselves suppress the resultant or
  ordinary source.
- EXT15 is a preprint theorem and P139 is conditional. Knight's “high cycle”
  is not every Collatz cycle.
- Resultant divisibility alone does not bound its magnitude below more strongly
  than `D`; an energy proof needs a uniform upper separation.
- Exact resonance is not a near-resonance inverse theorem. P155 remains useful
  arithmetic, but P158 rather than resonance now closes the area-three
  positive-cycle case.
- NG35: `75^7>3*64^7`; EXT05 plus factor separation alone stops at critical
  area five. Do not claim area at least seven from the same scalar comparison.
- NG36: P133's least-value rotation and P156's discrepancy-minimum rotation
  do not coincide for all positive rational shadows. An integer-cycle bridge
  must prove or explicitly transport between them.
- P164/P165 force growth but do not contradict either a tall profile with
  `h_*` of order at least `q^(1/3)` or a diffuse profile whose area is spread
  over many lower defects.
- NG37 prevents treating descent-density improvement as pointwise strict;
  NG38 requires the reduced endpoint term in every weighted polynomial
  estimate. The Phase 28 plateau shows that area can grow while `J,h` remain
  fixed.
- P173 closes exact coprime cancellation. Future proposed contradictions must
  therefore expose coefficient-growth or noncoprime arithmetic, not merely
  re-prove nonvanishing.
- NG40 prevents treating local exact-two incidence as a global near-grid; a
  proof must also handle positive-density residual exceptional contexts.

## First useful experiment

Start with P189/P190's exact dichotomy. In the grid-like subcase seek a
low-denominator pair-location resultant; in the residual-heavy subcase use
minimal nonsingleton P171 binomials. Separately cover full-`D` noncoprime
arithmetic. Search first for the smallest profile refuting the inequality and
rebuild NG34--NG40 plus the Phase 28--31 synthetic profiles before scaling.
Retain the endpoint correction and both rotations.
In parallel, test whether cyclotomic factors of the grouped noncoprime
polynomial recover a larger divisor of `D` than P140's `D_0`.

Run the trivial cycle and its powers, both negative cycles, Phase 24/25
low-area and resonance rows, the Phase 28 noncoprime corpus and synthetic
profiles, macro id0, NG28/NG30/NG32/NG34--NG40, and the mandatory
A/B families before scaling.

## Acceptance condition

An exact uniform theorem covering arbitrary defect area and all gcd classes,
with an effective finite remainder and independent verifier. It must conclude
either `abs(R)<D`, an incompatible stronger divisibility, or
`C_min<300000D` for every nontrivial positive profile.

## What this result would not separately establish

Even a complete H133 proof would not prove H70, H72, H112, H89, or the full
Collatz conjecture. `proves_collatz=false`.
