# H104 context pack — G270 positive-source geodesic exclusion

**Canonical status:** `OPEN`. The exact statement is the H104 row in
[`../CLAIMS_LEDGER.md`](../CLAIMS_LEDGER.md). The derivation is in the
[`Phase 17 audit`](../../research/audits/predecessor-pressure/REPORT.md).

## Target and boundary

Exclude every positive ordinary-source critical word in branch G270 whose
safe prefixes are all shortest in their same-Q endpoint classes. P104 then
eliminates the G270 side of a distinct-odd finite first crossing. This does
not address H105 or a repeated periodic orbit.

## Required inputs

- P89/P98: ancestral minimality and the exact geodesic criterion.
- P104: the G270/H270 dichotomy and all of its hypotheses.
- P105/P106: exact exponent pressure and the suffix-decodable finite subcode.
- E27--E29: finite geodesic, direct-orbit, predecessor, and adversarial data.
- NG17, P73, and NG24--NG29.
- Every mandatory adversarial family.

## Known traps

- Formal all-contact prefixes can be safe, geodesic, and 2-adically coherent.
- Summed Haar cylinder mass is not an ordinary representative count.
- NG29 caps the coefficient-only volume calculus; simply increasing inverse
  word depth does not supply an unbounded proof.
- A fixed positive ordinary source, affine correction, literal legality, and
  signed carry must not be dropped.
- P104 assumes distinct odd inputs and cannot be used on a repeated cycle.

## First useful experiment

Attach `(A,B,r2,r3,source order,signed carry)` to the suffix-decodable P106
state and search for a dominance rule that rejects a positive source. Before
scaling, require exact separation of NG24--NG29 and the formal all-contact
prefixes. Preserve the first collision between opposite dominance outcomes.

## Acceptance condition

An all-depth proof excluding every positive ordinary-source G270 word, with
an independently checkable finite remainder. A larger predecessor table,
finite pressure trend, or Haar-only estimate is insufficient.

`proves_collatz=false` until H105 and the separate periodic branch are also
resolved within a complete least-counterexample argument.
